document.addEventListener('DOMContentLoaded',()=>{const f=document.querySelector('[data-qualify-form]');const o=document.querySelector('[data-qualification-output]');if(f&&o){f.addEventListener('submit',e=>{e.preventDefault();const budget=(f.querySelector('[name="budget"]')?.value||'').toLowerCase();const timeline=(f.querySelector('[name="timeline"]')?.value||'').toLowerCase();const need=(f.querySelector('[name="need"]')?.value||'').toLowerCase();let tier='AI Data Starter';if(budget.includes('50')||budget.includes('100')||need.includes('api')||need.includes('recurring')) tier='Enterprise Intelligence';if(budget.includes('250')||need.includes('government')||need.includes('national')) tier='Sovereign Command';if((budget.includes('15')||need.includes('training')||timeline.includes('30'))&&tier==='AI Data Starter') tier='AI Training Dataset';o.innerHTML=`<div class="notice"><strong>Recommended path:</strong> ${tier}. Your request sounds like a fit for a structured proposal with dataset scope, licensing, delivery format, and timeline.</div>`;});}})